import React from "react";
import { View, Text, Image } from "react-native";

import logo from "../assets/logo.png";

export default function Login() {
    return <View>
        <Image source={logo} />
        <Text>
            EU FALEI QUE PERSISTENCIA TRAZ A CORREÇÃO DO ERRO
        </Text>
    </View>
}